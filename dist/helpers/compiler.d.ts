/**
 * Run the compiler on the specified path
 * @param rootDir The root directory to compile
 * @param options The compiler options
 */
export declare function runCompiler(rootDir?: string, options?: Record<string, any>): Promise<void>;
